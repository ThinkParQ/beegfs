# BeeGFS deeper cache daemon configuration.

# Set to "NO" to disable start of the BeeGFS deeper cache daemon via the init script.
START_SERVICE="YES"

# Set to "YES" if you want to start multiple BeeGFS deeper cache daemon with different
# configuration files on this machine.
#
# Create a subdirectory with the ending ".d" in "/etc/beegfs/" for every config
# file. The subdirectory name will be used to identify a particular daemon
# instance for init script start/stop.
#
# Note: The original config file in /etc/beegfs will not be used when multi-mode
# is enabled.
#
# Example: /etc/beegfs/scratch.d/beegfs-deeper-cached.conf
# $ /etc/init.d/beegfs-deeper-cached start scratch
MULTI_MODE="NO"
