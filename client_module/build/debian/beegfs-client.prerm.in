#!/bin/sh

CLIENT_DIR=/opt/beegfs/src/client/client_module___BEEGFS_DEB_MAJOR__

# try to clean up, but do not fail if it does not work
make -C ${CLIENT_DIR}/build/ clean --silent || true

#DEBHELPER#
